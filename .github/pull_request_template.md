## Summary

<!-- What changed, and why -- the problem or gap this closes, not just a
     restatement of the diff. Bullet points are fine. -->

## Test plan

<!-- What was actually run/verified before this PR was opened: tests,
     dry-runs, isolated fixture checks, manual verification steps. Evidence
     before assertions -- "should work" isn't a test plan. -->

- [ ]

<!--
This repo's release automation (.github/workflows/release.yml) resolves
the next version from labels on *merged* PRs via release-drafter
(.github/release-drafter.yml), auto-applied by
.github/workflows/autolabel.yml based on this PR's title/branch/files.
`major` / `minor` or `feature` / `fix` / `chore` are the labels that
matter; no label defaults to a patch bump. You don't need to add one by
hand unless the autolabeler mis-detects this PR's intent.
-->
